# Frontend (React)

This is the React single-page app for voice2summarizer.

## Setup

```bash
cd frontend
cp .env.example .env
npm install
npm run dev
```

The app will be available at `http://localhost:5173`.
